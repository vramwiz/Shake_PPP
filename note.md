# Shake_PPP 作業ノート

このファイルは `D:\DelphiProg\test\SYNC_Motion\note.md` から、このプロジェクトでも使える方針を抜き出し、Shake_PPPの仕様と現在の実装状態をまとめた作業資料である。

## プロジェクト構成

- `Shake_PPP.dpr` はDLLのexport境界と必要ユニットの列挙だけを担当する。
- `Source\Plugin\Filter\Shake_PPP_FilterPlugin.pas` はフィルター登録と映像処理の入口を担当する。
- `Source\Plugin\Filter\Shake_PPP_SettingsForm.pas/.dfm` は設定画面と画像プレビューを担当する。
- `Source\Common\Render\Shake_PPP_LastFrameCapture.pas` は `GetImageTexture2D` からフィルター処理前の入力画像を取得して最新フレームとして保持する。入力画像を取得できない場合だけフレームバッファへフォールバックする。
- 複数ユニットで共用する処理やSDK定義は `Source\Lib` に置く。
- 参照元から不要なユニットを一括で持ち込まず、実際に使う依存だけを置く。
- 現在の映像コールバックは画像を変形せず、設定プレビュー用にフィルター前の最新フレームだけを取得する。

## 半固形物の揺れ変形フィルター仕様

原案: `C:\Users\vramw\Downloads\半固形物_揺れ変形フィルター仕様.md`

### 目的と基本方針

- AviUtl2のフィルタープラグインとして、元画像内の指定範囲をプリンや水風船のような半固形物として変形させる。
- 対象物は壁や土台へ接続され、接続側は元画像へ追従し、内部は慣性と弾性によって遅れて追従して行き過ぎと戻りを表現する。
- 利用者が元画像を通常どおり上下左右へ移動・バウンドさせるだけで、その移動から自然な揺れを自動生成できることを理想とする。
- 元画像を背景とする専用GUIで対象形状を定義し、形状データと動画上で調整する揺れ設定を分離する。
- 静的変形と時間軸方向の揺れは、同じ形状データと内部変形構造を使用する。
- 正面と側面では形状解釈が異なるため、別の変形モデルとして扱う。

### 対象物の構造

- 土台・接続側: 壁または元画像へ接する部分。完全固定ではなく変位を小さくし、可動部分との境界が切れて見えないようにする。
- 弾性領域: 土台側と重心・頂点側をつなぎ、伸び、縮み、潰れ、反動を表現する。
- 重心・頂点範囲: 厚みが最大の部分または壁から最も張り出した部分。形状を崩しすぎず、揺れと周辺変形の基準にする。

### 変形モデル

正面モデル:

- 壁へ貼り付いた半球状の柔らかい物体を正面から見た構図とする。
- ひしゃげた形状にも合わせられる閉じた外周曲線と、内側の重心・頂点範囲を指定する。
- 外周から重心・頂点範囲へ向かう放射状の内部構造を生成する。
- 外周側の動きと重心側の遅れから、内部の伸縮と偏りを計算する。

側面モデル:

- 壁へ貼り付けた水風船を側面から見た、重力で下へ垂れた非対称な円錐状の構図とする。
- 壁に接する部分を接続側、壁から離れた先端付近を重心・頂点範囲とする。
- 土台から先端への方向と、重力による下方向への偏りを内部構造へ反映する。
- 必要に応じて、壁接続部を外周曲線上の区間として指定する。

モデル種別は時間軸上で変化させず、ベジェ曲線等の形状データとともに専用GUI側の情報として保存する。モデル変更は輪郭の意味と内部メッシュ生成方法を変えるため、AviUtl2の中間点対象にはしない。

### 専用形状編集GUI

専用GUIは、元画像を見ながら指定する必要がある形状情報だけを編集する。揺れの強さ、遅延、減衰等はAviUtl2側で設定する。

GUI項目:

- 変形モデル: 正面／側面
- 編集対象: 外周／重心・頂点範囲／壁接続部
- 操作モード: ベジェ曲線の追加・削除／編集
- 元に戻す／やり直す。ただし初期実装での優先度は低く、曲線編集と保存・復元を先に完成させる。
- 表示倍率
- 元画像／変形結果のプレビュー切り替え
- 動作確認モード。固定量または簡単な周期運動を与え、形状から生成した変形の動きを専用GUI内で確認する。
- 形状の初期化
- 壁接続部は側面モデルで必要な場合に表示する。

輪郭の初期入力:

1. 元画像上を順番にクリックして頂点を追加する。
2. 始点と現在の終点は異なる図形で表示し、ユーザーが区別できるようにする。
3. 最後の入力時に始点をダブルクリックすると始点へ接続して閉曲線とし、閉じた始点は四角で表示する。
4. 現在の終点をダブルクリックすると、始点へ接続せず開いた曲線として入力を確定する。
5. 開いた曲線の終点を始点へドラッグしてスナップさせた場合も閉曲線へ移行する。
6. 閉じている接続線を削除した場合は開いた曲線へ移行し、切断した両端を新しい始点・終点として表示する。
7. 頂点はユーザーが入力した配列順に接続し、自動的な並べ替えは行わない。
8. 頂点間は初期状態では自動的に滑らかな曲線で結ぶ。
9. 対象物全体の外周を入力する。
10. あまり動かさない重心・頂点範囲を同様に入力する。
11. 側面モデルでは必要に応じて壁との接続区間を指定する。

確定後は、頂点の追加・削除・移動、ベジェハンドルの移動、膨らみや凹みの調整、編集対象の切り替えを行えるようにする。通常は滑らかにつなぎ、必要な箇所だけハンドルを手動調整する。

想定マウス操作:

- 追加・削除モード: 左クリックで頂点追加、右クリックで頂点削除
- 編集モード: 左ドラッグで頂点またはベジェハンドルを移動
- 曲線確定: 始点のダブルクリックで閉じ、終点のダブルクリックで開いたまま確定
- 開閉変更: 終点を始点へ移動すると閉じ、閉じている接続線を削除すると開く
- 共通: ホイールで拡大・縮小、画像ドラッグで表示位置を移動
- 画像移動と頂点編集が競合しないよう、最終的なボタン割り当ては実装時に調整する。

### 専用GUI側の形状データ

次の情報は対象画像に対して一度定義するものであり、AviUtl2の通常設定値には表示しない。

- 変形モデル種別
- 対象物全体の外周ベジェ曲線
- 重心・頂点範囲のベジェ曲線
- 側面モデルの壁接続区間
- 壁または土台へ固定される範囲
- 頂点の基本位置と範囲
- 外周と重心・頂点範囲の対応関係
- 内部メッシュまたは内部変形構造
- 固定側から可動側へ移る影響分布
- 変形範囲外との境界処理情報
- 側面モデルの基準重力方向

固定度の分布は利用者へ細かく指定させず、外周、重心・頂点範囲、壁接続区間から内部的に自動生成することを基本とする。

### AviUtl2側の設定値

動画編集中に調整する値と、中間点で変化させる意味がある値をAviUtl2側へ置く。

基本設定候補:

- 時間軸計算の有効／無効
- 揺れの強さ
- 動きの遅れ
- やわらかさ
- 揺れの長さ・減衰
- 最大変形量
- 元画像の移動量に対する反応感度

初期の正面モデルでは重複を避け、AviUtl2側へ次の8項目を実装する。値は表示用の0～100%または0～200%から、処理用の倍率へ専用Settingsユニットで変換する。

- 時間軸計算
- 揺れの強さ（0～200%、初期値100%）
- 動きの遅れ（0～100%、初期値50%）
- やわらかさ（0～100%、初期値50%）
- 揺れの長さ（0～100%、初期値50%）
- 最大変形量（0～500px、初期値100px）
- 横方向（0～200%、初期値100%）
- 縦方向（0～200%、初期値100%）

重力、側面モデル固有方向、急停止時の反動、手動変形との混合は、初期の変形・時間軸計算を確認してから必要性を判断する。

方向設定候補:

- 横方向の影響率
- 縦方向の影響率
- 重力の強さ
- 揺れやすい方向

補助設定候補:

- 急停止時の反動の強さ
- 自動計算と手動変形の混合率
- 手動変形量または手動入力座標
- 揺れを停止する値
- 初期形状へ戻す値

物理用語をそのまま並べず、「やわらかさ」「動きの遅れ」「揺れの長さ」等、結果を想像しやすい名称を優先する。内部計算方式が決まった段階で重複を整理し、必要最小限に絞る。

### 時間軸計算

時間軸計算なし:

- 指定座標または手動変形量に基づき、そのフレームを直接変形する。
- 履歴と慣性を使用しない静的変形として動作する。

時間軸計算あり:

1. 毎フレーム、元画像の表示座標を取得する。
2. 前回フレームとの差から移動量と方向を求める。
3. 必要に応じてフレーム間隔から速度または加速度を求める。
4. 移動量を土台・壁側の動きとして弾性計算へ入力する。
5. 土台・壁側は元画像へ追従させる。
6. 重心・頂点範囲は慣性によって少し遅れて追従させる。
7. 弾性領域を伸縮・圧縮させ、土台側と重心側を滑らかにつなぐ。
8. 元画像停止後も、行き過ぎと戻りを減衰させながら計算する。
9. 最終的に初期形状へ戻す。

専用の揺れ用中間点を作らず、元画像の通常操作から自動的に揺れを生成できることを目標にする。

### 元画像から取得する情報

- 最優先は対象オブジェクトの毎フレームの表示座標とする。
- 表示座標を取得できれば移動による揺れを自動計算する。
- 拡大率と回転角への対応は将来追加できる構造とし、初期実装は移動座標だけを対象とする。
- 描画済み画像しか取得できない場合は座標の自動取得が難しいため、手動入力方式を使用する。

### フレーム履歴

- 先頭からの再生、任意位置へのシーク、フレーム飛び、逆方向、同一フレームの複数回呼び出し、プレビューと出力の呼び出し順の違いを考慮する。
- 前回フレームと連続していない場合は巨大な移動量を外力にせず、履歴を初期化する。
- 履歴初期化後は、そのフレームの静止形状を基準として計算を再開する。
- 同一フレームが複数回呼ばれた場合は時間を進めず、同じ結果を返すことを基本とする。

### 内部変形の要件

- 外周または壁接続側と変形範囲外の境界が切れて見えないこと。
- 固定側から可動側へ変位量を連続的に変化させること。
- 重心・頂点範囲は移動しても形状が崩れすぎないこと。
- 伸びる側だけでなく反対側の圧縮も表現すること。
- 大きな変形時の欠け、折り返し、過度な重なりを抑えること。
- 最大変形量で破綻を防ぐこと。
- 正面と側面で別々の内部変形規則を使用できること。
- ベジェ曲線から内部メッシュを生成する方法と画素補間方法は今後決定する。

### 保存データ

形状データの保存先にはAviUtl2の`data`項目を使用しない。`data`項目は制作者側で一時保存用途と考えられており、既知の不安要素と容量制約を避けるため、歌詞テロップオブジェクトと同じ方式を採用する。

- Filterの単一行`string`項目`形状データ`へ格納し、AviUtl2プロジェクトと一緒に保存・読込する。
- 改行を含まないバージョン付き文字列とし、現在の先頭識別子は`SPP2`とする。
- 現在の形式は `SPP2|1O,...|1C,...|2O,...|2C,...` とする。数字は形状セット、`O`は外周、`C`は重心・頂点範囲、座標は0～1の正規化値、頂点種別は鋭角／滑らかを表す。
- 初版の`SPP1|O,...|C,...`も読み込める。旧データはセット1へ復元し、セット2は空として扱い、次回保存時に`SPP2`へ更新する。
- 形状セット1・2で独立するのは外周と重心・頂点範囲の曲線データだけとし、時間軸計算、強さ、遅れ、やわらかさ、長さ、最大変形量、横方向、縦方向は共通設定とする。
- 利用者が直接編集する値ではなく、専用GUIを閉じるとシリアライズして書き戻す。
- Undo／RedoはAviUtl2側へ任せるため、専用GUIには保存・キャンセルボタンを置かず、タイトルバーを含む「閉じる」を保存として扱う。
- 解析・生成処理は保存先から独立した専用ユニットへ置き、将来の形式変更に備える。
- Windows単行Editの既定上限に合わせ、保存文字列は32,767文字以下に制限する。
- 保存前に上限を検査し、超過時は既存データを壊さずエラーを表示する。
- 読込時は識別子、バージョン、項目数、数値範囲、文字列終端まで検証し、破損データを描画処理へ渡さない。

形状データへ少なくとも次を保存する。

- データ形式のバージョン
- 変形モデル種別
- 外周曲線の頂点とベジェハンドル
- 重心・頂点範囲の頂点とベジェハンドル
- 各曲線の開閉状態
- 壁接続区間
- 内部生成に必要な補助情報

頂点とベジェハンドルは、フィルターへ現在届いている入力画像の幅と高さを基準にした正規化座標で保存する。編集画面の数値表示では現在の入力画像サイズを使ってピクセル座標へ換算する。基準画像サイズそのものは保存必須項目にせず、画像サイズ変更後は同じ正規化形状を新しい入力サイズへ比例適用する。将来の追加仕様に備えてバージョンを持ち、古い形状データを読み込める構造にする。プレビュー画像、Undo／Redo履歴、表示倍率、表示位置、自動生成した内部メッシュは保存せず、内部メッシュは保存曲線から再生成する。

### プレビュー

専用GUIでは次を切り替えて確認できるようにする。

- 元画像
- 外周曲線
- 重心・頂点範囲
- 壁接続区間
- 変形後画像

プレビューは形状確認を主目的とし、揺れの詳細調整はAviUtl2側で行う。必要であれば固定量による簡易変形プレビューだけを専用GUIへ追加する。

プレビュー実装は次の段階に分ける。

1. 曲線、頂点、ハンドル、半透明領域は編集操作へ常時追従させる。
2. 形状データの保存・復元後、時間軸を使わない固定量の静的変形プレビューを追加する。
3. 静的変形が成立した後、簡単な周期運動またはドラッグ入力による動作確認モードを追加する。
4. 変形処理が十分速い場合は、頂点・ハンドルのドラッグ中にも変形結果をリアルタイム更新する。
5. 処理時間が長い場合は、ドラッグ中だけ更新頻度またはメッシュ解像度を下げ、操作終了時に完全な品質で再計算する。

画像のズームと表示位置移動は表示座標だけを変更し、変形メッシュを再生成しない。生成済みの変形結果をキャッシュして再描画する。メッシュ再生成は形状、モデル種別、または変形条件が変わった場合に限定する。

### 実装前に確認・決定する事項

最優先:

- フィルタープラグインから元オブジェクトの表示座標を毎フレーム取得できるか。
- 取得できるフレーム番号または時刻情報の内容。
- 同一フレームが複数回処理される場合の識別方法。

変形方式:

- 正面／側面モデルそれぞれの内部変形規則。
- ベジェ曲線から変形用メッシュを生成する方法。
- 固定側から重心側までの影響率、画像補間、外周境界処理。

時間軸計算:

- 揺れ計算の物理モデルとフレームレート依存の扱い。
- シーク、逆再生、フレーム飛び時の履歴初期化条件。
- 揺れの停止と初期状態への復帰方法。

データとGUI:

- 形状データの保存形式と画像サイズ変更時の座標換算。
- 壁接続区間の指定方法。
- 簡易変形プレビューとマウス操作の最終仕様。

### 現段階の設計結論と次段階

- 壁または土台へ接続された半固形物を対象とする。
- 正面と側面を別モデルとして扱う。
- 外周と重心・頂点範囲をベジェ曲線で指定し、側面では必要に応じて壁接続区間も指定する。
- 形状編集は元画像を表示する専用GUIで行い、形状とモデル種別をGUI側データとして保存する。
- 揺れの強さ、遅れ、減衰等はAviUtl2側の設定値とする。
- 時間軸計算を無効にすれば静的変形、有効にすれば元画像のフレーム間移動から揺れを自動生成する。
- 非連続フレームでは履歴を初期化し、編集操作による異常な揺れを防ぐ。
- 次の設計段階では、元画像の表示座標を取得できるかを最初に確認し、その結果を踏まえて正面モデルの内部変形規則を具体化する。

### 表示座標取得の確認結果

- 映像フィルターコールバックの`OBJECT_INFO`で取得できるのは、オブジェクトID、フレーム、時刻、入力画像寸法、レイヤー等であり、標準描画のX/Y表示座標は含まれていない。
- プロジェクト編集用`EDIT_SECTION`にはオブジェクト設定を参照するAPIがあるが、これは編集コールバック用であり、毎フレームの映像処理へ表示座標を直接渡す仕組みではない。
- このため、元画像の通常移動からの自動揺れは、表示座標を安全に取得する別手段、または手動入力方式を決めてから実装する。
- 変形基盤は座標入力方式から独立させ、まず設定画面の固定量・静的変形プレビューで外周固定、重心範囲移動、弾性領域の連続補間を確認する。

## 共通ビルドルール

- Delphi 37.0を使用し、対象プラットフォームはWin64だけとする。
- DebugとReleaseのビルド設定を保つ。
- コンパイル警告とエラーを確認し、原則として警告0、エラー0で完了とする。
- ビルド前に `C:\ProgramData\aviutl2\Plugin\Shake_PPP` がなければ作成し、DLLを同フォルダーへ出力する。
- DebugはDLLを `Shake_PPP.auf2` へコピーし、調査用のDLLとRSMも残す。
- ReleaseはDLLを `Shake_PPP.auf2` へコピーした後、同じ出力先のDLLとRSMを削除する。

Debug Win64:

```powershell
cmd /c "call ""C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat"" && msbuild ""D:\DelphiProg\test\Shake_PPP\Shake_PPP.dproj"" /t:Build /p:Config=Debug /p:Platform=Win64"
```

Release Win64:

```powershell
cmd /c "call ""C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat"" && msbuild ""D:\DelphiProg\test\Shake_PPP\Shake_PPP.dproj"" /t:Build /p:Config=Release /p:Platform=Win64"
```

配備先:

```text
C:\ProgramData\aviutl2\Plugin\Shake_PPP\Shake_PPP.auf2
```

## 実装・保守ルール

- フィルターコールバック境界からDelphi例外を外へ漏らさない。
- 毎フレームの処理ではファイル再読込、不要なメモリ確保、GUI値の書き戻しを行わない。
- コメントは処理の言い換えではなく、目的、責務、注意点、状態や値の意味を補うために書く。
- DelphiのSDKレコードはC/C++側のABIと一致させ、フィールド追加時は順序、型、アラインメントを公式SDKと照合する。
- 責務が増えたら専用ユニットへ分け、グローバルな可変状態を避ける。
- 原因調査が必要な場合、Debugビルドだけ `C:\ProgramData\aviutl2\Plugin\Shake_PPP\Shake_PPP_debug.log` へ診断ログを出力してよい。Releaseビルドではログを出力しない。

## Git管理ルール

- `.pas`、`.dpr`、`.dproj`、`.res`、文書、配布・検証に必要なスクリプトと素材を同期対象とする。
- `Win32`、`Win64`、`.dcu`、`.rsm`、`.dll`、`.auf2`、IDEローカル設定、履歴・復旧データは同期しない。
- `.gitattributes` でPascal、プロジェクト、文書の改行をCRLFへ統一する。
- `.res` などのバイナリーファイルはbinaryとして扱う。

## 作業ログ

- 2026-08-06: SYNC_Motionを参考に、Win64 Debug/Release、ビルド後コピー、空フィルターを備えた最小プロジェクトを作成した。
- 2026-08-06: 起動時に設定項目がnilになるのを避けるため、未使用のダミートラック1項目とnil終端の項目配列を追加した。
- 2026-08-06: 設定ボタン、フィルター前画像の取得、拡大縮小・ドラッグ移動ができるプレビュー画面の土台を追加した。
- 2026-08-06: プレビューのドラッグ移動時のちらつきを抑えるため、オフスクリーン描画と不透明コントロールによるダブルバッファ表示へ変更した。
- 2026-08-06: 形状編集の開始点として、外周、重心範囲、画像移動、鋭角、滑らか、元画像、変形プレビュー、全体表示の手続き描画アイコンを持つ専用ツールバーを追加した。
- 2026-08-06: 外周／重心範囲の最小曲線エディターを追加した。画像基準の正規化座標で頂点を保持し、左クリック追加、頂点ドラッグ移動、右クリック削除、鋭角／滑らかの切替、選択曲線の強調表示に対応した。閉曲線操作と保存は次段階とする。
- 2026-08-06: 始点のダブルクリックと終点の始点へのスナップによる閉曲線化、終点のダブルクリックおよび閉じた接続線の右クリックによる開曲線化、閉曲線内部の半透明表示を追加した。
- 2026-08-06: 始点を大型の緑色マーカー（閉曲線時は二重枠）へ変更し、閉じた接続線をマゼンタ色で区別した。フォーム肥大化を抑えるため、ベジェ計算、描画、領域塗り、当たり判定を専用のCurveRendererユニットへ分離した。
- 2026-08-06: 2曲線をバージョン付き1行テキスト`SPP1`へ変換するCurveDataユニットを追加し、Filterの単一行string項目`形状データ`への保存・復元を実装した。現在は専用GUIを閉じたときに書き戻す。
- 2026-08-06: 非アクティブ曲線の線幅、明度、領域の不透明度を上げて視認性を改善した。Fit表示中の画像移動で倍率を100%へ切り替えないようにし、初回クリック時に拡大される現象を修正した。
- 2026-08-06: 移動モードと曲線編集モードのアイコン選択表示を排他的にした。初期状態および移動中は手だけを選択表示し、編集時は外周または重心範囲だけを選択表示する。
- 2026-08-06: ダミートラックを廃止し、時間軸計算、揺れの強さ、動きの遅れ、やわらかさ、揺れの長さ、最大変形量、横方向、縦方向の初期設定項目を追加した。SDK項目定義と処理用値への変換はFilterSettingsユニットへ分離した。
- 2026-08-06: `形状データ`をAviUtl2設定一覧の末尾へ移動した。編集フォームは閉じる操作を保存に統一して下部ボタンを撤去し、ズームスライダーを廃止してホイールと全体表示だけに整理した。フォームとツールバーへダーク配色を適用した。
- 2026-08-06: 初期操作を外周編集モードへ変更した。編集モードでも空所の右ドラッグで画像を移動でき、頂点の右クリック削除と両立する。閉じた接続線はドラッグせず右クリックした場合だけ開く。
- 2026-08-06: 旧移動モードを将来の動作プレビューモードとして予約し、手アイコンを再生アイコンへ変更した。このモード選択時に画像フィルターを有効化する設計とし、今回は説明表示だけを変更して動作ロジックは維持した。
- 2026-08-06: 映像コールバックの`OBJECT_INFO`には標準描画のX/Y表示座標が含まれないことをSDK型定義で確認した。変形処理の第一段階として、閉じた外周を固定境界、閉じた重心範囲を最大変位領域とする固定量の静的変形プレビューを追加した。境界間は距離に基づく滑らかな重みで逆引き変形し、結果を曲線変更までキャッシュする。動作プレビューモードへ入ると変形表示、編集モードへ戻ると元画像表示へ自動で切り替える。
- 2026-08-06: 1024x1024の静的変形が約13.6秒かかり設定画面が応答なしに見える問題を調査した。画素・色チャンネルごとの`TBitmap.ScanLine`参照を廃止して行ポインタをキャッシュし、4チャンネルを一度に補間するよう最適化した。同条件のDebugベンチマークは約250ms。Debugログへマスク生成、画像補間、合計時間を記録する計測を追加した。
- 2026-08-06: 動作プレビューモードへ周期的なX/Y変位を追加した。XとYで周期と位相を変え、方向変化を含む揺れを連続表示する。曲線から生成する画素ごとの変形マップをキャッシュし、各フレームは画像補間だけを実行する。1024x1024のDebugベンチマークはマップ生成約250ms、連続30フレームの平均約64ms。フレーム時間ログは約1秒ごとに制限する。
- 2026-08-06: 動作プレビューのX/Y入力を無限の周期運動から減衰振動へ変更した。再生開始時の振幅へ指数減衰を掛け、X/Yで異なる周期の行き過ぎと戻りを表示する。残り振幅が0.2px未満になったらX=0、Y=0を明示的に描画して停止し、変形前の状態へ収束させる。収束後は再生アイコンで再実行できる。
- 2026-08-06: 動作プレビューの入力仕様を、再生開始時の自動振動からプレビュー上の左ドラッグへ変更した。ドラッグ移動量を画像座標へ換算してX/Y速度へ外力として加え、ボタンを離した後は減衰ばねで行き過ぎながら変形前の状態へ収束する。右ドラッグと中ボタンは表示位置移動として残す。
- 2026-08-06: 動作プレビューの左ドラッグを、変形への入力だけでなく画像の表示移動も同時に行う操作へ修正した。画像の表示位置をドラッグへ追従させ、そのフレーム間移動量を揺れのX/Y外力へ渡す。クリックだけでは入力せず、3px以上のドラッグから画像移動と揺れを開始する。右ドラッグは揺れを発生させない表示移動として扱う。
- 2026-08-06: 動作プレビューの上下方向がドラッグ方向と逆だったため、画面座標の下向き正と`TBitmap`走査線の下から上への並びを考慮し、揺れへ渡すY移動量の符号を反転した。左右方向は変更しない。
- 2026-08-06: 外周と重心・頂点範囲の組を2セット保持できるようにした。ツールバーのセット1／2で編集・プレビュー対象を切り替え、他方の曲線は保持する。保存形式を`SPP2|1O|1C|2O|2C`へ拡張し、旧`SPP1`はセット1として互換読込する。AviUtl2側の揺れ設定値は両セット共通とする。
- 2026-08-06: ツールバーの選択表示を、各操作から個別更新する方式から内部状態を基に一括更新する方式へ変更した。形状セット1／2、外周／重心／動作プレビュー、鋭角／滑らか、元画像／変形表示の各グループで必ず1項目だけを選択表示する。
- 2026-08-06: ツールバーの選択色が解除後も残り、複数選択に見える原因を修正した。アイコン描画後に透明化されたブラシを次の再描画前に不透明へ戻し、古い背景色を必ず消去する。加えてツールバー部品自身に排他グループを持たせ、セット、編集対象、頂点種別、表示の各グループでクリック時にも1項目だけが選択されるよう二重に保証した。ホバー色は選択色と区別した。
- 2026-08-06: 動作プレビューボタンだけに残っていたオン／オフ反転処理を廃止し、排他グループに合わせてクリック時は必ず動作プレビューモードを選択するよう統一した。再クリックでも解除せず、外周・重心ボタンまたは元画像表示で編集モードへ戻る。
- 2026-08-06: 動作プレビュー開始後にツールバーの排他操作が壊れる原因を修正した。50msタイマーの左ボタン監視がツールバー上のクリックまで画像ドラッグと誤認し、`PreviewPaintBox`へマウスキャプチャを奪って各ボタンの`MouseUp`を失わせていた。ドラッグ監視の開始をプレビュー領域内に限定し、開始済みの画像ドラッグだけ領域外へ継続できるようにした。
- 2026-08-06: AviUtl2設定上部へ`移動X`・`移動Y`トラック（-10000～10000、刻み0.1）を追加し、実際の映像フィルター処理を開始した。値をキャラクター側の移動座標と同じにし、連続フレームの座標差で半固形部分の仮想位置を本体から遅らせる。右移動では左へ残り、座標停止後は内部速度で右へ行き過ぎ、ばねと減衰で変形0へ戻る。同一フレームは物理状態を再計算せず、逆再生・シーク・フレーム飛びでは現在座標を新しい基準として履歴を初期化する。保存済みの2形状セットへ共通の変位を順に適用し、変形前の入力画像を設定画面用に保持する。
- 2026-08-06: `移動X`・`移動Y`の受信状態を解析できるよう、Debugビルドでは映像コールバックごとにオブジェクトID、Effect ID、フレーム、時刻、時間軸設定、トラックの生値、処理へ渡す座標値を診断ログへ記録するようにした。Releaseビルドでは記録処理をコンパイル対象外とする。
- 2026-08-06: 再生負荷による小さなフレーム飛びのたびに揺れがリセットされていたため、10フレーム以内の前方飛びは前回座標から今回座標までを線形補間し、欠落フレーム数だけばね・減衰計算を進める方式へ変更した。逆再生と10フレームを超える移動はシークとして履歴を初期化する。Debugログにはフレーム間隔と1フレーム当たりの補間移動量も記録する。
- 2026-08-06: 4px間隔の変形マスクを補間した際にベジェ外周の外側へ非ゼロの重みがにじんでいたため、全画素ウェイトの生成時に元の外周ポリゴンで厳密な内外判定を行い、外側を必ず0へ固定するようにした。プレビューと実映像処理は同じ変形マップを使用するため、両方へ適用される。
- 2026-08-06: 曲線座標と実際の変形範囲のずれを調査できるよう、Debugビルドの変形マップ生成時に外周・重心曲線の全頂点を正規化座標と画像ピクセル座標で記録し、サンプリング後のベジェ境界および非ゼロウェイトの実画素範囲もログへ出すようにした。変形マップ座標はDPI非依存の画像座標であることも明記する。
- 2026-08-06: 座標ログで設定曲線と生成ウェイトのY範囲が一致している一方、実映像だけ上側が動くことを確認した。変形ウェイトはVCL `TBitmap.ScanLine`に合わせた下端起点、AviUtl2のRGBAバッファは上端起点であるため、実映像用`ApplyRgba`だけウェイト行を上下反転して参照するよう修正した。X座標と設定データは変更しない。
- 2026-08-06: 実映像のY範囲修正後も設定画面の変形範囲が上下反転していたため、VCLプレビュー用`Apply`も実映像と同じ上端起点の曲線座標へ統一した。ウェイト行を上下反転して参照し、縦方向の逆引きサンプリング符号も`ApplyRgba`と一致させた。固定量プレビューと動作プレビューの両方へ適用される。
- 2026-08-06: 形状セット1／2を切り替えた際に、別の排他グループである動作プレビューが解除されて外周編集・元画像表示へ戻る問題を修正した。セット切替は対象曲線の参照と変形マップだけを更新し、外周／重心／動作プレビュー、元画像／変形表示、頂点種別の各選択状態を変更しない。
- 2026-08-06: SDK v2.13で追加された`get_output_image_param`と現在フィルターの相対画像パラメータ`param`をDelphi SDK定義へ追加した。実映像の揺れ入力は画像出力項目のX/Yと相対X/Yを合成した元オブジェクト座標へ切り替えた。既存の`移動X`・`移動Y`設定はプロジェクト互換のため残し、画像パラメータを取得できない対象でのみフォールバックとして使用する。Debugログには取得元、画像出力座標、相対座標、実際に使用した座標を記録する。
- 2026-08-06: オブジェクト座標へ切り替えた映像が動かなかったため診断ログを確認した。`get_output_image_param`は成功しているが、サンプルでは全フレームで出力X/Yと相対X/Yがともに0だった。ログへ取得関数の有無、取得成否、相対パラメータの有無、オブジェクトのレイヤー・エフェクトレイヤー・フラグも追加し、0が取得失敗なのか実際の取得値なのかを判別できるようにした。
- 2026-08-06: 閉曲線の先頭だけ鋭角に見える表示を修正した。末尾から先頭へ戻るマゼンタ線の太さを他のベジェ区間と統一し、閉曲線時の先頭マーカーを塗りつぶしから二重の輪郭表示へ変更した。先頭の識別表示は残しつつ、マーカーが滑らかな接続部分を覆わないようにした。ベジェ形状と変形範囲の計算は変更していない。
- 2026-08-06: 複雑な形状編集と揺れ設定を利用者向けに説明する`README.md`を追加した。導入、最短手順、ツールバーとマウス操作、形状作成のコツ、全AviUtl2設定、調整例、トラブルシューティング、現在の制限、開発者向けビルド手順を現在の実装に合わせて記載した。
- 2026-08-06: `SYNC_Motion`の配布構成を基に`Setup`フォルダーを追加した。`make_release_zip.bat`からPowerShellスクリプトを呼び出し、Release版`Shake_PPP.auf2`と`README.md`を`Shake_PPP`フォルダーへまとめた`Shake_PPP.zip`を生成する。Debug用DLL/RSMが残っている場合は誤配布防止のため中止する。GitHub Release向けの`Shake_PPP.catalog.json`も追加した。
- 2026-08-06: AviUtl2カタログv0.3.3の登録画面でGitHub Owner・Repo・正規表現の3項目が読み込まれなかったため、`installer.source`から新形式の`type/owner/repo/pattern`混在部分を削除した。v0.3.3のスキーマとインストール済みカタログ実例に合わせ、`source.github.owner/repo/pattern`だけを持つ形式へ統一した。
